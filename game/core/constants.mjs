export const VERSIONS = Object.freeze({
  schema: 1,
  rules: 'ver1.0.0',
  scenario: 'kurikara-1.0.0'
});

export const CALENDAR = Object.freeze({
  startYear: 2029,
  startMonth: 4,
  totalTicks: 60
});

export const LIMITS = Object.freeze({
  residentsMin: 1000,
  residentsMax: 2000,
  relationMin: -1,
  relationMax: 1
});
