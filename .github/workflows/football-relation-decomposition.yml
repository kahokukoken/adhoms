name: ADHOMS Football Relation Decomposition
on:
  push:
    paths:
      - 'research/football/event_engine_relation_decomposition.py'
      - 'research/football/event_engine.py'
      - '.github/workflows/football-relation-decomposition.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --upgrade pip numpy pandas requests scikit-learn
      - run: PYTHONPATH=research/football python research/football/event_engine_relation_decomposition.py
      - run: cat research/football/output/event_engine_relation_decomposition.json
      - uses: actions/upload-artifact@v4
        with:
          name: adhoms-football-relation-decomposition
          path: research/football/output/event_engine_relation_decomposition.json
          if-no-files-found: error
          retention-days: 30
