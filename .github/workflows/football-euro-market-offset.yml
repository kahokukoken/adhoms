name: ADHOMS Football Euro Market Offset Validation
on:
  push:
    paths:
      - 'research/football/euro_market_offset_validation.py'
      - '.github/workflows/football-euro-market-offset.yml'
  workflow_dispatch:
permissions:
  contents: read
env:
  PYTHONUNBUFFERED: '1'
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pip pandas numpy requests scipy scikit-learn
      - name: Run frozen market-offset validation
        run: python research/football/euro_market_offset_validation.py
      - run: cat research/football/output/euro_market_offset_validation.json
      - uses: actions/upload-artifact@v4
        with:
          name: adhoms-football-euro-market-offset
          path: research/football/output/euro_market_offset_validation.json
          if-no-files-found: error
          retention-days: 30
