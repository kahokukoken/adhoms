# ADHOMS Playtest v0.7 Import Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Recover the approved ADHOMS Ver.1 TGS Playtest v0.7 as the repository’s public prototype baseline without exposing ADHOMS Core internals.

**Architecture:** Keep the recovered prototype as a single static `index.html` so its UI and presentation-only behavior remain unchanged. Treat GitHub as the implementation source of truth; documentation will state that the public prototype is not the ADHOMS Core implementation.

**Tech Stack:** Static HTML, CSS, vanilla JavaScript, GitHub Pages-compatible repository layout.

**Spec:** Notion `ADHOMS Game Design — Rev.0.1` and `ADHOMS Game Revision History` (TGS playtest v0.7 direction).

## Global Constraints

- Preserve the approved FIELD TERMINAL smartphone UI direction.
- Preserve FEED-centric interaction and monthly review overlay.
- Public repository contains presentation-only prototype logic, not ADHOMS Core.
- Do not add API keys, internal prompts, private model parameters, or non-public design documents.
- Keep the prototype runnable as a static page with no build step.

---

### Task 1: Recover the v0.7 prototype

**Files:**
- Create: `index.html`

**Interfaces:**
- Consumes: recovered File Library artifact titled `ADHOMS Ver.1 TGS Playtest v0.7`.
- Produces: static browser entry point `index.html`.

- [ ] Recover the complete HTML/CSS/JS artifact.
- [ ] Commit it unchanged except for reconstruction required by File Library chunking.
- [ ] Verify the document has a doctype, matching closing tags, and the v0.7 title/build label.
- [ ] Verify required interactive functions exist: feed rendering, research, weekly/monthly advancement, monthly meeting, and five-year ending.

### Task 2: Make repository purpose explicit

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: `index.html`.
- Produces: public repository landing page and run instructions.

- [ ] Describe ADHOMS at a high level without disclosing Core internals.
- [ ] Identify `index.html` as the public prototype baseline.
- [ ] Add local run instructions and GitHub Pages intent.
- [ ] State explicitly that the public repository does not contain ADHOMS Core.

### Task 3: Verify import and open a reviewable change

**Files:**
- Read: `index.html`, `README.md`

**Interfaces:**
- Produces: reviewable pull request from `playtest-v0.7-import` to `main`.

- [ ] Read back both files from GitHub.
- [ ] Confirm no secrets or private Core materials were added.
- [ ] Confirm the v0.7 title and terminal closing script/body/html are present.
- [ ] Open a pull request summarizing the recovery and verification.
